meta {
  name: create user
  type: http
  seq: 2
}

post {
  url: {{host}}/users
  body: json
  auth: inherit
}

body:json {
   {
     "email": "user@email.com",
     "name" : "John Doe",
     "password": "password"
   }
}
